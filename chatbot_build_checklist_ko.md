# 수출 데이터 챗봇 구축 체크리스트 (한국어)

## 1) 지금 당장 해야 할 진단 (현재 단계)
- [ ] **질문 유형 20개 샘플 수집**: 실제 미팅에서 나오는 질문을 원문 그대로 모으기
- [ ] **용어 사전 1차본 작성**: 실무 용어 ↔ Raw 컬럼명 매핑 테이블 만들기
- [ ] **필수 계산식 고정**: CIF/FOB/공헌% 등 계산 로직을 코드/SQL로 중앙화
- [ ] **정답 검증셋 만들기**: 20~50개 질문에 대해 기대 정답과 근거 시트 기록
- [ ] **데이터 업데이트 주기 정의**: 일/주/월 배치 기준과 책임자 지정

## 2) 권장 아키텍처 (GPT만으로 하지 않기)
- [ ] **RAG + 계산 엔진 분리**
  - 텍스트 질의 해석: LLM
  - 수치 조회/집계: SQL 엔진(예: DuckDB/Postgres)
  - 계산식: Python/SQL 함수로 구현
- [ ] **원천 데이터 계층화**
  - Raw: 원본 그대로 보관
  - Standard: 컬럼명/단위/코드 표준화
  - Mart: 질의에 필요한 집계 테이블 (월/분기/연)
- [ ] **툴 호출 구조**
  1. `parse_question` (기간/국가/회사/지표 추출)
  2. `map_terms` (용어 매핑)
  3. `query_metric` (SQL 조회)
  4. `compute_metric` (공식 적용)
  5. `cite_evidence` (파일/시트/조건 명시)

## 3) Tool 설계 체크리스트 (필수)
- [ ] 입력 스키마 JSON 고정 (`year`, `period_type`, `country`, `company`, `metric`)
- [ ] 출력 스키마 JSON 고정 (`value`, `unit`, `filters`, `source_file`, `source_sheet`)
- [ ] `25`는 `2025`가 아닌 **문자열 그대로** 처리 규칙 반영
- [ ] 단위 일원화: 물량은 항상 `MT`
- [ ] 집계 규칙: 월/분기/년 합산 규칙 명시
- [ ] 계산 실패 시 에러코드 반환 (`MISSING_COLUMN`, `ZERO_DIVISION`, `NO_DATA`)

## 4) 프롬프트 설계 체크리스트
- [ ] **절대 추측 금지** 문구 명시
- [ ] 답변 템플릿 강제:
  1) 결론 수치
  2) 비교/해석
  3) 근거(파일명·시트명·필터조건·컬럼)
  4) 계산식
- [ ] 모호 질의 시 반문 규칙(예: 국가/기간 미지정)
- [ ] Few-shot 10개 이상: 성공/실패/예외 케이스 포함
- [ ] "질의 의도 분류"를 먼저 수행 후 툴 호출

## 5) 데이터 정합성/매핑 관리
- [ ] 용어 동의어 테이블 운영 (예: "수출실적"→`매출_수익률`/`수출실적_한국`)
- [ ] 컬럼 표준명 사전 (`판매량_mt`, `fob_usd`, `cif_usd` 등)
- [ ] 매핑 테이블 버전 관리 (Git)
- [ ] 신규 용어 발견 시 등록 프로세스 (요청→검토→반영)

## 6) 성능 개선 체크리스트 (30초→3~8초 목표)
- [ ] 자주 묻는 질의 사전 집계(Materialized View)
- [ ] 기간/국가/회사 인덱스 생성
- [ ] 응답 2단계: "우선 수치" + "상세 근거 후속"
- [ ] LLM 컨텍스트 최소화(원본 전체 투입 금지)
- [ ] 대용량은 파일 직접 참조 대신 DB 적재 후 조회

## 7) 엑셀 + 텍스트 확장 대비
- [ ] 텍스트 문서는 임베딩 인덱스 구축 (정책/계약/메모)
- [ ] 숫자 질의는 **반드시 SQL/계산엔진** 우선
- [ ] 하이브리드 라우팅: 숫자질문→DB, 설명질문→RAG
- [ ] 증거 분리 표기: `정량 근거` / `정성 근거`

## 8) 운영 체크리스트
- [ ] 배치 성공/실패 알람 (Slack/Email)
- [ ] 데이터 품질 테스트 (NULL 비율, 음수/이상치)
- [ ] 월간 정답률 리포트 (정확도/재현율)
- [ ] 사용자 피드백 루프(오답 사례 자동 수집)

## 9) 최소 기능 제품(MVP) 2주 로드맵
- [ ] 1주차: 데이터 표준화 + 매핑 사전 + 20개 검증셋
- [ ] 2주차: 툴 체인(parse/map/query/compute) + 답변 템플릿 + 모니터링

## 10) 시작용 시스템 프롬프트 초안
"당신은 한국 제지 수출 분석 보조 AI다. 수치 질문은 반드시 데이터 조회 툴과 계산 툴로 처리하고, 추측하지 않는다. 답변 시 파일명, 시트명, 기간/국가/회사 필터, 사용 컬럼, 계산식을 반드시 표시한다. 데이터가 없으면 없다고 답한다. 물량 단위는 MT로 통일한다. 연도 토큰은 사용자 입력 그대로 해석한다(예: 25는 25)."
